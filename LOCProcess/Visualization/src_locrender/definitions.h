#pragma once
#ifndef DEFINITIONS_H
#define DEFINITIONS_H

    #define PI 3.14159265358979f
    #define sigScaler 4.0f
    #define MINSIG 0.5f
    
    #ifndef max
        #define max(a,b) (((a) > (b)) ? (a) : (b))
    #endif

    #ifndef min
        #define min(a,b) (((a) < (b)) ? (a) : (b))
    #endif

#endif

